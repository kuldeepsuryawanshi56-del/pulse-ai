{
	"name": "pulse-ai",
	"displayName": "Pulse AI",
	"publisher": "glie",
	"description": "Operational memory for vibe coding teams — capture decisions, dead-ends and patterns from AI coding sessions automatically.",
	"version": "0.1.25",
	"license": "SEE LICENSE IN LICENSE",
	"repository": {
		"type": "git",
		"url": "https://github.com/glieai/pulse-ai"
	},
	"homepage": "https://pulse.glie.ai",
	"icon": "resources/icon.png",
	"engines": {
		"vscode": "^1.100.0"
	},
	"categories": ["Other"],
	"keywords": ["ai", "coding", "knowledge-base", "documentation", "insights"],
	"activationEvents": ["onStartupFinished"],
	"main": "./dist/extension.js",
	"contributes": {
		"configuration": {
			"title": "Pulse",
			"properties": {
				"pulse.llmProvider": {
					"type": "string",
					"enum": ["auto", "anthropic", "openai"],
					"default": "auto",
					"description": "LLM provider for insight generation. 'auto' uses Claude Code / Codex CLI"
				},
				"pulse.llmApiKey": {
					"type": "string",
					"default": "",
					"description": "API key for direct LLM fallback. Not needed if Claude Code or Codex is installed"
				},
				"pulse.llmModel": {
					"type": "string",
					"default": "",
					"description": "Model override (e.g. claude-sonnet-4-5-20250929). Leave empty for defaults"
				},
				"pulse.watcherSizeThresholdKb": {
					"type": "number",
					"default": 256,
					"minimum": 32,
					"description": "Session file growth (in KB) before auto-generating an insight draft"
				},
				"pulse.watcherPollInterval": {
					"type": "number",
					"default": 5000,
					"minimum": 1000,
					"description": "How often to check session file size (in ms)"
				}
			}
		},
		"viewsContainers": {
			"activitybar": [
				{
					"id": "pulse",
					"title": "Pulse",
					"icon": "resources/icon.svg"
				}
			]
		},
		"views": {
			"pulse": [
				{
					"id": "pulse.watcher",
					"name": "Watcher"
				},
				{
					"id": "pulse.drafts",
					"name": "Drafts"
				},
				{
					"id": "pulse.searchResults",
					"name": "Search"
				},
				{
					"id": "pulse.recentInsights",
					"name": "Recent"
				}
			]
		},
		"commands": [
			{
				"command": "pulse.search",
				"title": "Pulse: Search Knowledge Base",
				"icon": "$(search)"
			},
			{
				"command": "pulse.insight",
				"title": "Pulse: Create Insight",
				"icon": "$(sparkle)"
			},
			{
				"command": "pulse.push",
				"title": "Pulse: Publish & Push",
				"icon": "$(cloud-upload)"
			},
			{
				"command": "pulse.deleteDraft",
				"title": "Pulse: Delete Draft",
				"icon": "$(trash)"
			},
			{
				"command": "pulse.publishDraft",
				"title": "Pulse: Publish Draft",
				"icon": "$(cloud-upload)"
			},
			{
				"command": "pulse.openDraft",
				"title": "Pulse: Open Draft"
			},
			{
				"command": "pulse.configure",
				"title": "Pulse: Configure"
			},
			{
				"command": "pulse.setupMcp",
				"title": "Pulse: Setup MCP in this folder"
			},
			{
				"command": "pulse.refresh",
				"title": "Pulse: Refresh",
				"icon": "$(refresh)"
			},
			{
				"command": "pulse.openInsight",
				"title": "Pulse: Open Insight"
			},
			{
				"command": "pulse.watchStart",
				"title": "Pulse: Start Watcher",
				"icon": "$(play)"
			},
			{
				"command": "pulse.watchStop",
				"title": "Pulse: Stop Watcher",
				"icon": "$(debug-stop)"
			},
			{
				"command": "pulse.watchToggle",
				"title": "Pulse: Toggle Watcher"
			}
		],
		"keybindings": [
			{
				"command": "pulse.search",
				"key": "ctrl+shift+k",
				"mac": "cmd+shift+k"
			}
		],
		"menus": {
			"view/title": [
				{
					"command": "pulse.watchStart",
					"when": "view == pulse.watcher && !pulse.watcherRunning",
					"group": "navigation"
				},
				{
					"command": "pulse.watchStop",
					"when": "view == pulse.watcher && pulse.watcherRunning",
					"group": "navigation"
				},
				{
					"command": "pulse.insight",
					"when": "view == pulse.drafts",
					"group": "navigation"
				},
				{
					"command": "pulse.push",
					"when": "view == pulse.drafts",
					"group": "navigation"
				},
				{
					"command": "pulse.refresh",
					"when": "view == pulse.drafts",
					"group": "navigation"
				},
				{
					"command": "pulse.search",
					"when": "view == pulse.searchResults",
					"group": "navigation"
				},
				{
					"command": "pulse.refresh",
					"when": "view == pulse.recentInsights",
					"group": "navigation"
				}
			],
			"view/item/context": [
				{
					"command": "pulse.publishDraft",
					"when": "view == pulse.drafts && viewItem == draft",
					"group": "inline@1"
				},
				{
					"command": "pulse.deleteDraft",
					"when": "view == pulse.drafts && viewItem == draft",
					"group": "inline@2"
				}
			]
		},
		"viewsWelcome": [
			{
				"view": "pulse.watcher",
				"contents": "Auto-generate insights as you code.\n[$(play) Start Watcher](command:pulse.watchStart)",
				"when": "!pulse.watcherRunning"
			}
		]
	},
	"scripts": {
		"build": "node esbuild.mjs --production",
		"watch": "node esbuild.mjs --watch",
		"typecheck": "tsc --noEmit",
		"test": "echo 'no tests yet'"
	},
	"devDependencies": {
		"@types/node": "^25.5.0",
		"@types/vscode": "^1.100.0",
		"esbuild": "^0.25.0"
	}
}
